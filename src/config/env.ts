const appwrite_id = String(process.env.NEXT_PUBLIC_APPWRITE_PROJECT_ID);
const appwrite_url = String(process.env.NEXT_PUBLIC_APPWRITE_URL);
const appwrite_key = String(process.env.APPWRITE_API_KEY);

export { appwrite_id, appwrite_url, appwrite_key };
